

const config = {
    APP_NAME: import.meta.env.VITE_APP_NAME,
    APP_API_URL: import.meta.env.VITE_APP_API_URL, 
    OAUTH_GOOGLE: import.meta.env.VITE_OAUTH_GOOGLE,
    OAUTH_EFARMER:import.meta.env.VITE_OAUTH_EFARMER
}

export default config;
